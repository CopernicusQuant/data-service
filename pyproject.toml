[project]
name = "copernicus_quant_data_service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "fastmcp>=3.4.7",
    "google-cloud-firestore>=2.29.0",
    "pyarrow>=25.0.1",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.3",
    "tushare>=1.4.29",
    "uvicorn[standard]>=0.52.4",
]

[tool.ty.environment]
python = ".venv"

# [tool.pytest.ini_options]
# log_cli = true
# log_cli_level = "INFO"
